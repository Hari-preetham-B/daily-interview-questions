# 2026-09-19 PM — DSA (Medium)

## Question
Given an integer array `height` of length `n`, where each element represents the height of a vertical line drawn at coordinate `(i, height[i])`, find two lines that together with the x-axis form a container that holds the most water. Return the maximum area of water the container can store. Note that you may not slant the container.

## Hint
Use a two-pointer approach starting at opposite ends of the array. To maximize area, moving the pointer with the shorter height is the only way that might yield a larger area.

## Answer
We can solve this problem in $O(n)$ time using the two-pointer technique. We initialize two pointers, `left` at index `0` and `right` at index `n - 1`, and maintain a variable `max_area` set to `0`. At each step, the current area is calculated as `(right - left) * min(height[left], height[right])`. To potentially find a larger area, we must increment `left` if `height[left] < height[right]`, or decrement `right` otherwise, because moving the taller line can never increase the area bounded by the shorter line. We repeat this until the pointers meet, requiring $O(1)$ extra space.

python
def maxArea(height: list[int]) -> int:
    left, right = 0, len(height) - 1
    max_area = 0
    while left < right:
        current_area = (right - left) * min(height[left], height[right])
        max_area = max(max_area, current_area)
        if height[left] < height[right]:
            left += 1
        else:
            right -= 1
    return max_area

